<%= form_with(model: discussion, local: true) do |form| %>
  <% if discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(discussion.errors.count, "error") %> prohibited this discussion from being saved:</h2>

      <ul>
      <% discussion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :handline %>
    <%= form.text_field :handline, id: :discussion_handline %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :discussion_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
